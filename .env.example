# ====================================
# Docker Compose 배포 설정
# ====================================
# AWS RDS 엔드포인트 (프로덕션)
# 예: smartmealtable-db.c1234567890.ap-northeast-2.rds.amazonaws.com
RDS_ENDPOINT=your-rds-endpoint-here

# DB 사용자명
DB_USERNAME=smartmeal_user

# DB 패스워드 (강력한 패스워드 사용)
DB_PASSWORD=your-secure-password-here

# Redis 호스트 (Admin 인스턴스 Private IP)
REDIS_HOST=admin-instance-private-ip

# Redis 포트
REDIS_PORT=6379

# Admin 서버 공개 IP (프로덕션)
ADMIN_PUBLIC_IP=your-admin-public-ip

# Admin 서버 Private IP (프로덕션)
ADMIN_PRIVATE_IP=your-admin-private-ip

# ====================================
# External Client 설정
# ====================================
# 이 설정들은 smartmealtable-core/src/main/resources/application-client.yml 에서 사용됩니다.
# 실제 값은 .env 파일에 설정하세요 (Git에 커밋하지 마세요!)

# Kakao OAuth
KAKAO_CLIENT_ID=your_kakao_client_id_here
KAKAO_REDIRECT_URI=http://localhost:5173/oauth/kakao/callback

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:5173/oauth/google/callback

# Naver Maps API
NAVER_MAP_CLIENT_ID=your_naver_cloud_api_id
NAVER_MAP_CLIENT_SECRET=your_secret

# Vertex AI (Google Cloud) - 지출 내역 파싱용
VERTEX_AI_PROJECT_ID=your_vertex_ai_project_id_here
VERTEX_AI_MODEL=gemini-2.5-flash
VERTEX_AI_TEMPERATURE=0.1
VERTEX_AI_LOCATION=asia-northeast3

# ====================================
# JWT 설정
# ====================================
# JWT 시크릿 키 (최소 256비트 필수 - 32글자 이상)
# 프로덕션에서는 안전한 난수 키를 사용하세요
JWT_SECRET=your_secure_jwt_secret_key_minimum_256_bits_32_characters_or_longer

# JWT SECRET KEY
JWT_SECRET=mySecretKeyForJWTTokenGenerationThatShouldBeLongEnough

# production, dev, stage, local 환경의 변수를 적절히 설정하세요
