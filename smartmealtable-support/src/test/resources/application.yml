spring:
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 3000
      lettuce:
        pool:
          max-active: 10
          max-idle: 5
          min-idle: 2

logging:
  level:
    root: INFO
    com.stdev.smartmealtable: DEBUG
