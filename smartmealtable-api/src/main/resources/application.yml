spring:
  application:
    name: smartmealtable-api

  # 프로필 설정 (client, monitoring)
  profiles:
    include: client,monitoring

  # Database 설정
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/smartmealtable?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root123}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  # JPA 설정
  jpa:
    database: mysql
    # Hibernate가 자동으로 MySQL 방언 선택하도록 함 (MySQL8Dialect는 deprecated)
    hibernate:
      ddl-auto: validate  # 운영: validate, 개발: update
    properties:
      hibernate:
        format_sql: true
        show_sql: false
        use_sql_comments: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true
    open-in-view: false

  # Jackson 설정
  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: Asia/Seoul
    default-property-inclusion: always  # null 값도 JSON에 포함

# JWT 설정
jwt:
  secret: ${JWT_SECRET:mySecretKeyForJWTTokenGenerationThatShouldBeLongEnough}
  access-token-expiry: 3600000  # 1시간 (밀리초)
  refresh-token-expiry: 86400000  # 24시간 (밀리초)

# Logging 설정
logging:
  level:
    root: INFO
    com.stdev.smartmealtable: INFO
    org.springframework.web: WARN
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql.BasicBinder: WARN
    # Tomcat HTTP 프로세서 로그 제어 (EOFException 등 불필요한 연결 에러 필터링)
    org.apache.coyote.http11.Http11Processor: WARN
    org.apache.catalina.connector.CoyoteAdapter: WARN
    # Spring Data JPA NamedQuery 조회 경고 억제
    org.springframework.data.jpa.repository.query.NamedQuery: WARN
    # Hibernate 리소스 트랜잭션 로깅 억제 (NamedQuery 조회 시 의도적 예외)
    org.hibernate.resource.transaction.backend.jdbc.internal.JdbcResourceLocalTransactionCoordinatorImpl: WARN
    # Tomcat 클래스 로더 ClassNotFoundException 억제 (Java 22 호환성 클래스, Java 21에서는 무해함)
    org.springframework.boot.loader.net.protocol.jar.JarUrlClassLoader: ERROR

# Server 설정
server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

# Actuator 설정 (모니터링 엔드포인트)
management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  endpoint:
    health:
      show-details: always

---
spring:
  config:
    activate:
      on-profile: local
  jpa:
    hibernate:
      ddl-auto: create
logging:
  level:
    root: DEBUG