# deploy-docs.sh 개선 사항 요약

## 📋 개선 내용

### 문제점
기존 `deploy-docs.sh` 스크립트는 **모든 REST Docs 테스트를 매번 실행**하여 배포할 때마다 5-10분이 소요되었습니다.

### 솔루션
이제 **수정된 테스트만 선택적으로 실행**할 수 있도록 개선되었습니다! 🚀

---

## ✨ 새로운 기능

### 1. 자동 감지 모드 (가장 권장) ⭐

```bash
./deploy-docs.sh --auto-detect
```

**동작**:
- Git의 변경사항을 자동으로 감지
- 수정된 파일과 연관된 RestDocsTest만 실행
- **소요 시간: 1-2분**

**예시**:
```bash
git add .
./deploy-docs.sh --auto-detect
```

---

### 2. 특정 테스트 필터링

```bash
# 단일 테스트
./deploy-docs.sh --test-filter 'AuthControllerRestDocsTest'

# 여러 테스트
./deploy-docs.sh --test-filter '*AuthControllerRestDocsTest|*SignupControllerRestDocsTest'

# 패턴 매칭
./deploy-docs.sh --test-filter 'com.stdev.smartmealtable.api.auth.*'
```

**소요 시간**: 30초 ~ 2분 (테스트 개수에 따라)

---

### 3. 사용 가능한 테스트 확인

```bash
./deploy-docs.sh --list-tests
```

출력:
```
AddressControllerRestDocsTest
AppSettingsControllerRestDocsTest
AuthControllerRestDocsTest
...
```

---

### 4. 테스트 스킵 (가장 빠름)

```bash
./deploy-docs.sh --skip-tests
```

**용도**: 이미 생성된 snippets를 재사용하여 HTML만 생성  
**소요 시간**: ~30초

---

## 🎯 사용 방법

| 명령어 | 소요 시간 | 상황 |
|--------|----------|------|
| `./deploy-docs.sh` | 5-10분 | 전체 검증 (초기 배포) |
| `./deploy-docs.sh --auto-detect` | 1-2분 | 일반적인 배포 (권장) ⭐ |
| `./deploy-docs.sh --test-filter 'TestName'` | 30초-2분 | 특정 기능만 수정 |
| `./deploy-docs.sh --skip-tests` | ~30초 | 문서 구조만 변경 |

---

## 📚 문서

### 빠른 시작
`docs/DEPLOY_DOCS_QUICK_START.md` - 5분 내에 이해할 수 있는 가이드

### 상세 가이드
`docs/DEPLOY_DOCS_GUIDE.md` - 모든 옵션과 예시를 포함한 완전한 가이드

---

## 🔧 구현 상세

### 추가된 함수

1. **`get_modified_tests()`**
   - Git diff로 수정된 파일 감지
   - RestDocsTest 클래스명 추출
   - 여러 테스트를 `|`로 연결하여 반환

2. **`list_available_tests()`**
   - 프로젝트의 모든 RestDocsTest 파일 나열
   - 정확한 테스트 이름 확인에 사용

### 추가된 옵션

```bash
--test-filter <PATTERN>   # 특정 테스트 패턴 지정
--auto-detect            # Git 변경사항 기반 자동 감지
--list-tests             # 사용 가능한 테스트 목록 표시
--skip-tests             # 테스트 스킵 (기존 옵션)
--help, -h               # 도움말 표시
```

---

## 💡 활용 팁

### Alias 설정

`~/.zshrc`에 다음을 추가하면 더 빠르게 사용 가능:

```bash
alias dd='./deploy-docs.sh --auto-detect'
alias dd-all='./deploy-docs.sh'
alias dd-list='./deploy-docs.sh --list-tests'
alias dd-skip='./deploy-docs.sh --skip-tests'
```

사용:
```bash
dd              # 수정된 테스트만 배포
dd-all          # 전체 테스트 배포
dd-list         # 사용 가능한 테스트 확인
dd-skip         # 테스트 스킵
```

### CI/CD 통합

GitHub Actions에서:
```yaml
- name: Deploy REST Docs
  run: ./deploy-docs.sh --auto-detect
```

---

## ✅ 체크리스트

- [x] 자동 감지 모드 구현
- [x] 테스트 필터링 기능
- [x] 사용 가능한 테스트 목록 표시
- [x] 도움말 및 사용 예시 추가
- [x] 빠른 시작 가이드 작성
- [x] 상세 가이드 작성

---

## 🚀 다음 단계

1. **빠른 시작**: `docs/DEPLOY_DOCS_QUICK_START.md` 읽기
2. **실습**: `./deploy-docs.sh --auto-detect` 실행해보기
3. **커스터마이징**: 필요에 따라 `--test-filter` 옵션 사용

---

## 📝 참고

- **기존 기능 유지**: 기존 사용법 (`./deploy-docs.sh`)은 그대로 작동합니다
- **하위 호환성**: 모든 기존 스크립트와 호환됩니다
- **개선 사항**: 더 빠른 배포와 유연한 선택이 가능해졌습니다

---

**핵심 메시지**: `./deploy-docs.sh --auto-detect` 하나로 대부분의 배포 작업을 간편하게 처리할 수 있습니다! 🎉
